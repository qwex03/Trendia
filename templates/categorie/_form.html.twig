{{ form_start(form) }}
    <div class="space-y-4">
        {{ form_row(form.name, {
            'attr': {
                'class': 'w-full px-4 py-2 rounded-lg bg-neutral-800 text-white border border-neutral-700 focus:outline-none focus:ring-2 focus:ring-white',
                'placeholder': 'Entrez le nom de la catégorie'
            }
        }) }}
        <button type="submit" 
                class="w-full py-2 bg-white text-black rounded-lg shadow hover:bg-neutral-200 transition">
            {{ button_label|default('Enregistrer') }}
        </button>
    </div>
{{ form_end(form) }}
